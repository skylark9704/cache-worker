{
  "name": "cache-worker",
  "version": "1.0.0",
  "description": "Cloudflare worker for fallback routing",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
